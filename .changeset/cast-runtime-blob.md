---
"@patterkit/ops": patch
---

The inlined playable-runtime snapshot is refreshed for the cast APIs (`getCast`, `castForScene`,
`castForBlock`), so a playable HTML export ships a runtime that has them.
