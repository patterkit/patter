{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "noEmit": true
  },
  "include": ["src/renderer/src", "src/renderer/play", "src/renderer/search", "src/renderer/coverage", "src/renderer/preview", "src/shared"],
  "_comment": "The root config excludes this app's main / preload / renderer so its pure-Node program skips them. `exclude` is INHERITED through `extends` and resolved against the ROOT, so without this override it also excluded everything below - and these two programs silently checked nothing but src/shared.",
  "exclude": ["node_modules"]
}
