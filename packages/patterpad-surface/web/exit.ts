// The exit half of the shared "panel" motion vocabulary is the shell's (ui-review-2026-09, finding 5):
// the shell's `exit.ts` header says it was this file, lifted, and the family keeps one copy of the
// twelve lines that play `--anim-panel-out`. The `./exit.js` import path stays for the surface's popups.
export { closeWithExit } from "@wildwinter/app-shell";
