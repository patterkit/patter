{
  "name": "patterkit-monorepo",
  "private": true,
  "type": "module",
  "description": "The Patter open core: language, compiler, runtime, and CLI (@patterkit/*).",
  "license": "MIT",
  "author": "Ian Thomas",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "git config core.hooksPath scripts/git-hooks || true",
    "typecheck": "tsc --noEmit && tsc --noEmit -p examples/player/tsconfig.json && tsc --noEmit -p packages/patterpad/tsconfig.node.json && tsc --noEmit -p packages/patterpad/tsconfig.web.json && tsc --noEmit -p packages/patterpad-surface/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:pad": "npm run dev -w @patterkit/patterpad",
    "build": "npm run build -w @patterkit/model -w @patterkit/core -w @patterkit/dialect -w @patterkit/compiler -w @patterkit/runtime -w @patterkit/ops -w @patterkit/play-helpers -w @patterkit/cli",
    "bump:play": "node scripts/bump-play-version.mjs",
    "bump:pad": "node scripts/bump-patterpad-version.mjs",
    "release:pad": "node scripts/release.mjs pad",
    "release:play": "node scripts/release.mjs play",
    "release:cli": "node scripts/release.mjs cli",
    "release:guard": "node scripts/release-guard.mjs",
    "release:status": "node scripts/release-status.mjs",
    "release:npm": "node scripts/release-npm.mjs",
    "ship:cli": "node scripts/ship-cli.mjs",
    "ship:npm": "node scripts/ship-npm.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "overrides": {
    "esbuild": "^0.28.2"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^3.2.6"
  }
}
